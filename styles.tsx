import { StyleSheet } from 'react-native';

const styles = {
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },

  title: {
    textAlign: 'center' as 'center',
    fontSize: 32,
    color: "#000",
  },

  text: {
    fontSize: 20,
  }
};

export default styles;